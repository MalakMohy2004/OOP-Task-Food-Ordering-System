Food Ordering System
A simple Java console app that lets a customer view a menu, add items to an order, choose a payment method, see the total price, and get an order summary with a notification. Built as an OOP practice project — it uses encapsulation, inheritance, polymorphism, and the Strategy design pattern for handling different payment methods (card, installment via Valu, and cash).
